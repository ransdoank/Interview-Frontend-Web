require("dotenv");

module.exports = {
  // url: process?.env?.REACT_APP_API_SERVER_URL ?? 'https://cors-anywhere.herokuapp.com/https://screening.moduit.id'
  url: process?.env?.REACT_APP_API_SERVER_URL ?? 'https://screening.moduit.id'
};
